// Deprecated in favor of src/config/wallet.ts
// This file is kept only to avoid breaking imports that may exist outside the
// current code, but all runtime configuration now lives in wallet.ts.

export * from './wallet'
